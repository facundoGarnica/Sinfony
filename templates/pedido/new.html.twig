{% extends 'base.html.twig' %}

{% block title %}New Pedido{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('Css/StyleForNew.css') }}">
    <div class="container">
        <h1>Crear nuevo Pedido</h1>

        {{ include('pedido/_form.html.twig') }}

        <a href="{{ path('app_pedido_index') }}" class="back-link">Volver a la lista</a>
    </div>
{% endblock %}
